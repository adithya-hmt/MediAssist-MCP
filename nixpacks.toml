[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install -r apps/api/requirements.txt"]

[start]
cmd = "cd apps/api && python3 -m uvicorn main:app --host 0.0.0.0 --port $PORT"
